import { deleteCookie, getCookie, setCookie } from "hono/cookie";
import { Hono } from "hono";

export type PostStatus = "draft" | "published";
export type ApplicationStatus = "new" | "reviewing" | "accepted" | "rejected";
export interface Post { id:string; title:string; body:string; slug:string; tags:string[]; status:PostStatus; pinned:boolean; createdAt:string; updatedAt:string; }
export interface Application { id:string; name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; status:ApplicationStatus; createdAt:string; updatedAt:string; }
export interface ApplicationInput { name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; }
export interface PostInput { id?:string; title:string; body:string; slug?:string; tags:string[]; status:PostStatus; pinned:boolean; }
export interface Repository {
  listPosts(): Promise<Post[]>; listApplications(): Promise<Application[]>; createApplication(input: ApplicationInput): Promise<void>;
  upsertPost(input: PostInput): Promise<void>; deletePost(id: string): Promise<void>; updateApplicationStatus(id: string, status: ApplicationStatus): Promise<void>;
}
export interface AppEnv { Bindings: Env; }

const fallbackPosts: Post[] = [{
  id: "seed",
  title: "主站正在成形",
  body: "这里会持续发布站点更新、上线记录、实验想法与合作动态。它不是传统博客，更像我的品牌流与项目日志。",
  slug: "seed",
  tags: ["brand", "动态"],
  status: "published",
  pinned: true,
  createdAt: "2026-03-22T08:00:00.000Z",
  updatedAt: "2026-03-22T08:00:00.000Z",
}];

const css = `:root{--paper:#f6fff7;--mist:#eefcf1;--mint:#dff7d2;--apple:#baf08f;--apple-deep:#7dc65d;--ink:#102117;--muted:rgba(16,33,23,.62);--line:rgba(16,33,23,.1);--glass:rgba(255,255,255,.46);--glass-strong:rgba(255,255,255,.7);--shadow:0 36px 90px rgba(55,113,62,.08);--radius-xl:42px;--radius-lg:30px;--radius-md:18px;--gutter:clamp(18px,4vw,60px);--content:1360px;--hero-shift:0px;--hero-tilt:0deg;--hero-fade:1;--transition:300ms cubic-bezier(.22,1,.36,1)}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;font:16px/1.6 "Segoe UI Variable Text","PingFang SC","Microsoft YaHei UI",sans-serif;color:var(--ink);background:radial-gradient(circle at 16% 8%,rgba(255,255,255,.96),transparent 28%),radial-gradient(circle at 82% 18%,rgba(189,242,155,.52),transparent 22%),radial-gradient(circle at 62% 54%,rgba(227,251,215,.9),transparent 26%),linear-gradient(180deg,var(--paper),var(--mist) 42%,#e4f6e7);min-height:100vh;overflow-x:hidden}a{text-decoration:none;color:inherit}button,input,textarea,select{font:inherit}button{appearance:none;background:none}img,svg{display:block;max-width:100%}.page{width:min(100%,var(--content));margin:0 auto;padding:0 var(--gutter) 88px}.site-nav{position:fixed;top:18px;left:50%;transform:translateX(-50%);width:min(calc(100% - 28px),920px);display:flex;justify-content:space-between;align-items:center;gap:18px;padding:14px 18px;z-index:30;border:1px solid rgba(255,255,255,.82);border-radius:999px;background:rgba(255,255,255,.4);backdrop-filter:blur(28px) saturate(145%);box-shadow:0 14px 40px rgba(78,132,84,.06)}.site-nav nav,.actions,.meta,.toolbar,.inline,.tags{display:flex;gap:12px;align-items:center;flex-wrap:wrap}.mark,.eyebrow,.smallcaps{font-size:11px;letter-spacing:.22em;text-transform:uppercase;color:var(--muted)}.site-nav a,.site-nav button,.ghost-link,.toolbar a,.toolbar button{border:0;background:none;color:var(--muted);padding:4px 0;transition:color var(--transition),opacity var(--transition)}.site-nav a:hover,.site-nav button:hover,.ghost-link:hover,.toolbar a:hover,.toolbar button:hover{color:var(--ink)}.hero{position:relative;min-height:100svh;padding:112px 0 44px;display:grid;grid-template-columns:minmax(0,.84fr) minmax(420px,1fr);gap:clamp(28px,5vw,96px);align-items:end}.hero-copy{position:relative;z-index:2;padding-bottom:clamp(20px,4vw,56px);transform:translateY(calc(var(--hero-shift) * -.28));opacity:var(--hero-fade)}.hero-copy h1,.section-title,.footer-title,.admin-pane h2,.login-panel h2,.modal-panel h2{font-family:Georgia,"Times New Roman",serif;letter-spacing:-.075em;line-height:.88;margin:0}.hero-copy h1{font-size:clamp(5rem,14vw,11rem)}.hero-copy p{max-width:16rem;margin:18px 0 0;color:var(--muted);font-size:clamp(1rem,1.2vw,1.08rem)}.actions{margin-top:26px}.hero-meta{margin-top:22px;display:flex;gap:10px;flex-wrap:wrap}.hero-meta span,.pill,.tag{display:inline-flex;align-items:center;min-height:30px;padding:0 12px;border-radius:999px;background:var(--glass);backdrop-filter:blur(14px);box-shadow:inset 0 0 0 1px rgba(255,255,255,.82);font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.hero-visual{position:relative;min-height:calc(100svh - 156px);display:grid;align-items:stretch;transform:translateY(calc(var(--hero-shift) * .5)) rotate(var(--hero-tilt))}.poster{position:relative;min-height:100%;border-radius:54px;overflow:hidden;background:linear-gradient(160deg,rgba(255,255,255,.72),rgba(235,255,229,.3) 34%,rgba(196,247,166,.32) 68%,rgba(255,255,255,.42));border:1px solid rgba(255,255,255,.88);box-shadow:var(--shadow);backdrop-filter:blur(30px) saturate(145%)}.poster::before,.poster::after{content:"";position:absolute;pointer-events:none}.poster::before{inset:-8% 24% 50% -4%;background:linear-gradient(135deg,rgba(255,255,255,.95),rgba(255,255,255,0));transform:rotate(-14deg)}.poster::after{right:-14%;top:12%;width:280px;height:280px;border-radius:50%;background:rgba(185,240,143,.42);filter:blur(40px)}.poster-frame{position:absolute;inset:18px;border-radius:40px;border:1px solid rgba(255,255,255,.84)}.poster-grid{position:absolute;inset:0;overflow:hidden}.poster-grid::before,.poster-grid::after{content:"";position:absolute;height:1px;left:8%;right:8%;background:linear-gradient(90deg,rgba(255,255,255,.08),rgba(255,255,255,.72),rgba(255,255,255,.08))}.poster-grid::before{top:25%}.poster-grid::after{top:61%}.blob-a,.blob-b,.blob-c,.blade-a,.blade-b{position:absolute}.blob-a,.blob-b,.blob-c{border-radius:56% 44% 52% 48%/44% 54% 46% 56%;filter:blur(.2px)}.blob-a{inset:12% 14% 20% 26%;background:linear-gradient(145deg,rgba(255,255,255,.78),rgba(223,249,205,.84) 34%,rgba(179,233,145,.74) 66%,rgba(255,255,255,.28));box-shadow:inset 0 0 0 1px rgba(255,255,255,.7),0 28px 70px rgba(124,185,98,.18)}.blob-b{inset:26% 24% 16% 42%;background:linear-gradient(155deg,rgba(255,255,255,.16),rgba(133,209,103,.56));filter:blur(10px)}.blob-c{inset:8% 44% 36% 10%;background:linear-gradient(150deg,rgba(255,255,255,.86),rgba(238,255,230,.18));transform:rotate(-18deg)}.blade-a,.blade-b{border-radius:999px;background:linear-gradient(180deg,rgba(255,255,255,.76),rgba(255,255,255,.06));mix-blend-mode:screen}.blade-a{top:-6%;bottom:24%;left:46%;width:1px;transform:rotate(22deg)}.blade-b{top:22%;bottom:-8%;left:58%;width:1px;transform:rotate(-18deg)}.poster-caption{position:absolute;left:26px;right:26px;bottom:26px;display:flex;justify-content:space-between;align-items:end;gap:18px}.poster-caption strong{font-size:clamp(1.6rem,2.1vw,2.4rem);font-weight:600}.poster-caption span{display:block;color:var(--muted);font-size:13px}.section{padding:56px 0 0}.section[data-reveal]{opacity:.01;transform:translateY(34px);transition:transform 820ms cubic-bezier(.22,1,.36,1),opacity 820ms cubic-bezier(.22,1,.36,1)}.section.is-visible[data-reveal]{opacity:1;transform:none}.journal-shell{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(220px,.45fr);gap:30px;align-items:end;padding:36px 0 0;border-top:1px solid var(--line)}.section-title{font-size:clamp(2.6rem,5vw,4.5rem)}.section-copy{max-width:18rem;margin:10px 0 0;color:var(--muted)}.journal-card{padding:34px;border-radius:var(--radius-xl);background:linear-gradient(180deg,rgba(255,255,255,.76),rgba(239,255,235,.46));border:1px solid rgba(255,255,255,.86);box-shadow:var(--shadow);backdrop-filter:blur(28px) saturate(145%)}.journal-card h3{margin:16px 0 10px;font-size:clamp(2rem,3.5vw,3.4rem);line-height:.95;letter-spacing:-.05em}.journal-card p{margin:0;max-width:42rem;color:var(--muted);font-size:clamp(1rem,1.3vw,1.12rem)}.journal-aside{display:grid;gap:10px;align-content:end}.journal-date{font-family:Georgia,"Times New Roman",serif;font-size:clamp(2.4rem,5vw,4rem);letter-spacing:-.08em;line-height:.9}.cooperate{padding:40px 0 0;border-top:1px solid var(--line);display:flex;justify-content:space-between;gap:24px;align-items:end;flex-wrap:wrap}.cooperate p{max-width:20rem;margin:0;color:var(--muted)}.btn,button.btn{display:inline-flex;align-items:center;justify-content:center;min-height:52px;padding:0 22px;border-radius:999px;border:0;cursor:pointer;transition:transform var(--transition),background var(--transition),color var(--transition),box-shadow var(--transition)}.btn:hover,button.btn:hover{transform:translateY(-1px)}.btn-primary{background:#16241b;color:#f8fff6;box-shadow:0 20px 44px rgba(22,36,27,.14)}.btn-secondary{background:rgba(255,255,255,.6);box-shadow:inset 0 0 0 1px rgba(255,255,255,.82);backdrop-filter:blur(18px)}.page-footer{padding-top:34px}.footer-grid{display:grid;grid-template-columns:minmax(0,.9fr) minmax(0,.7fr);gap:18px}.footer-block,.admin-pane,.login-panel,.modal-panel{background:linear-gradient(180deg,rgba(255,255,255,.72),rgba(245,255,244,.4));border:1px solid rgba(255,255,255,.88);border-radius:var(--radius-lg);padding:clamp(22px,3vw,34px);box-shadow:var(--shadow);backdrop-filter:blur(28px) saturate(140%)}.footer-block p,.admin-pane p{margin:0;color:var(--muted)}.footer-title{font-size:clamp(2rem,4vw,3rem);margin-bottom:8px}form{display:grid;gap:14px}label{display:grid;gap:8px;font-size:14px}input,textarea,select{width:100%;padding:14px 16px;border-radius:var(--radius-md);border:1px solid rgba(110,174,94,.18);background:rgba(255,255,255,.86);color:var(--ink)}textarea{min-height:140px;resize:vertical}.split{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.msg{padding:12px 14px;border-radius:var(--radius-md);font-size:14px}.ok{background:rgba(27,91,78,.1);color:#1b5b4e}.err{background:rgba(139,45,29,.1);color:#8b2d1d}.modal{position:fixed;inset:0;display:grid;place-items:center;padding:20px;opacity:0;visibility:hidden;pointer-events:none;transition:opacity var(--transition),visibility var(--transition);z-index:60}.modal.is-open{opacity:1;visibility:visible;pointer-events:auto}.modal-backdrop{position:absolute;inset:0;background:rgba(240,251,241,.56);backdrop-filter:blur(24px)}.modal-panel{position:relative;width:min(100%,760px);max-height:min(88vh,920px);overflow:auto}.modal-head{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;margin-bottom:18px}.modal-head p{margin:10px 0 0;color:var(--muted)}.icon-btn{width:42px;height:42px;border-radius:999px;background:rgba(255,255,255,.68);box-shadow:inset 0 0 0 1px rgba(255,255,255,.82)}.admin-page,.login-page{min-height:100svh}.admin-top,.login-shell{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap;padding:24px var(--gutter) 34px}.toolbar form{display:block}.workspace{display:grid;grid-template-columns:minmax(320px,.92fr) minmax(0,1.08fr);gap:20px}.stack,.list{display:grid;gap:18px}.admin-pane h2,.login-panel h2{font-size:clamp(2rem,4vw,3rem);margin-top:10px}.mini{padding:18px 0;border-top:1px solid var(--line)}.mini:first-child{padding-top:0;border-top:0}.mini p{margin:0 0 10px}.check{width:20px;height:20px}.login-panel{width:min(100%,520px);margin:0 var(--gutter)}@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}*,*::before,*::after{animation:none!important;transition:none!important}}@media (max-width:980px){.hero,.journal-shell,.workspace,.footer-grid{grid-template-columns:1fr}.hero{padding-top:106px;min-height:auto}.hero-visual{min-height:480px}.cooperate{align-items:start}}@media (max-width:720px){.page{padding:0 18px 72px}.site-nav{top:12px;width:calc(100% - 20px);padding:14px 16px;border-radius:28px}.hero{grid-template-columns:1fr;padding-top:100px}.hero-copy h1{font-size:clamp(4rem,22vw,6rem)}.hero-visual{min-height:360px}.poster{border-radius:32px}.poster-frame{inset:14px;border-radius:24px}.poster-caption{left:18px;right:18px;bottom:18px}.split{grid-template-columns:1fr}.login-panel{margin:0 18px}}`;

const esc = (v:string) => v.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;");
const slugify = (v:string) => v.trim().toLowerCase().replace(/[^a-z0-9\u4e00-\u9fa5\s-]/g,"").replace(/\s+/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"") || `post-${Math.random().toString(36).slice(2,8)}`;
const tags = (v:string|string[]) => (Array.isArray(v)?v:v.split(",")).map((x)=>x.trim()).filter(Boolean).slice(0,6);
const shell = (title:string, body:string) => `<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>${esc(title)}</title><meta name="description" content="juren233.top 的主域名门户，展示合作入口与最新动态。"/><style>${css}</style></head><body>${body}</body></html>`;
const brandArtwork = () => `<div class="poster" id="abstract-brand-mark" aria-hidden="true"><div class="poster-frame"></div><div class="poster-grid"></div><div class="blob-a"></div><div class="blob-b"></div><div class="blob-c"></div><div class="blade-a"></div><div class="blade-b"></div><div class="poster-caption"><div><strong>juren233</strong><span>poster landing</span></div><span>2026</span></div></div>`;

function home(posts: Post[]) {
  const lead = (posts.length ? posts : fallbackPosts)[0];
  const leadDate = new Date(lead.createdAt).toLocaleDateString("zh-CN");
  const leadHtml = `<article class="journal-card"><div class="meta">${lead.pinned ? '<span class="pill">Pinned</span>' : ""}<span>${leadDate}</span></div><h3>${esc(lead.title || "未命名动态")}</h3><p>${esc(lead.body)}</p><div class="tags">${lead.tags.map((t)=>`<span class="tag">${esc(t)}</span>`).join("")}</div></article>`;
  return shell("juren233.top | 主域名门户", `<main class="page"><header class="site-nav"><div><div class="mark">Main portal</div><div class="smallcaps">juren233</div></div><nav><a href="#journal">动态</a><button type="button" data-open-modal="cooperation-modal">合作申请</button><a href="/admin">后台</a></nav></header><section class="hero"><div class="hero-copy"><div class="eyebrow">Main domain</div><h1>juren233.top</h1><p>一个像海报一样的首页，只保留品牌、最新更新和一个入口。</p><div class="actions"><button class="btn btn-primary" type="button" data-open-modal="cooperation-modal">打开合作申请</button></div><div class="hero-meta"><span>apple green</span><span>single update</span></div></div><div class="hero-visual">${brandArtwork()}</div></section><section class="section" id="journal" data-reveal><div class="journal-shell"><div><div class="eyebrow">Journal</div><h2 class="section-title">最新动态</h2><p class="section-copy">首页只展示最新一条记录。</p>${leadHtml}</div><aside class="journal-aside"><div class="eyebrow">Latest entry</div><div class="journal-date">${leadDate}</div><div class="mark" style="letter-spacing:.08em;text-transform:none;">只保留一条真正需要被看见的更新。</div></aside></div></section><section class="section" id="cooperate" data-reveal><div class="cooperate"><div><div class="eyebrow">Collaboration</div><h2 class="section-title">合作申请</h2><p>通过一张冷白玻璃信息牌承接合作表单。</p></div><button class="btn btn-primary" type="button" data-open-modal="cooperation-modal">打开合作申请</button></div></section><footer class="page-footer"><div class="footer-grid"><section class="footer-block"><div class="eyebrow">Identity</div><h2 class="footer-title">juren233.top</h2><p>主站入口与更新公告。</p></section><section class="footer-block"><div class="eyebrow">Contact</div><p>合作申请统一通过弹窗表单提交。</p></section></div></footer></main><div class="modal" id="cooperation-modal" aria-hidden="true"><div class="modal-backdrop" data-close-modal></div><section class="modal-panel" role="dialog" aria-modal="true" aria-labelledby="cooperation-title"><div class="modal-head"><div><div class="eyebrow">Collaboration request</div><h2 id="cooperation-title">合作申请</h2><p>提交后会直接进入后台列表。</p></div><button class="icon-btn" type="button" aria-label="关闭合作申请" data-close-modal>×</button></div><form id="cooperation-form"><label>申请人名称<input name="name" placeholder="怎么称呼你" required/></label><label>联系方式<input name="contact" placeholder="邮箱 / QQ / Telegram / 微信" required/></label><label>项目说明<textarea name="projectSummary" placeholder="介绍你的内容、用途和展示方式" required></textarea></label><div class="split"><label>预期子域名 / 用途说明<input name="requestedSubdomain" placeholder="例如 demo、lab、share-guest"/></label><label>备注<input name="notes" placeholder="可填写时效、风格或其他说明"/></label></div><button class="btn btn-primary" type="submit">提交申请</button><div id="form-message" class="mark" style="letter-spacing:.08em;text-transform:none;">申请会直接进入后台列表。</div></form></section></div><script>const root=document.documentElement;const modal=document.getElementById("cooperation-modal");const openers=document.querySelectorAll('[data-open-modal="cooperation-modal"]');const closers=document.querySelectorAll("[data-close-modal]");const form=document.getElementById("cooperation-form");const message=document.getElementById("form-message");const reduced=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const setModal=(open)=>{if(!modal)return;modal.classList.toggle("is-open",open);modal.setAttribute("aria-hidden",String(!open));document.body.style.overflow=open?"hidden":"";};openers.forEach((node)=>node.addEventListener("click",()=>setModal(true)));closers.forEach((node)=>node.addEventListener("click",()=>setModal(false)));window.addEventListener("keydown",(event)=>{if(event.key==="Escape")setModal(false);});if(!reduced){const onScroll=()=>{const progress=Math.min(window.scrollY/880,1);root.style.setProperty("--hero-shift",String(progress*48)+"px");root.style.setProperty("--hero-tilt",String(progress*-2.8)+"deg");root.style.setProperty("--hero-fade",String(1-progress*.08));};onScroll();window.addEventListener("scroll",onScroll,{passive:true});const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting)entry.target.classList.add("is-visible");});},{threshold:.2});document.querySelectorAll("[data-reveal]").forEach((node)=>observer.observe(node));}else{document.querySelectorAll("[data-reveal]").forEach((node)=>node.classList.add("is-visible"));}form?.addEventListener("submit",async(event)=>{event.preventDefault();if(!message)return;const data=new FormData(form);const payload=Object.fromEntries(data.entries());message.textContent="正在提交申请...";try{const response=await fetch("/api/applications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const json=await response.json();if(!response.ok)throw new Error(json.error||"提交失败");form.reset();message.textContent="申请已提交成功，我会在后台查看并跟进。";}catch(error){message.textContent=error instanceof Error?error.message:"提交失败，请稍后重试。";}});</script>`);
}

function login(error?: string) {
  return shell("Admin Login", `<main class="page login-page"><header class="login-shell"><div><div class="mark">Admin access</div><div class="smallcaps">juren233 / backstage</div></div><a class="ghost-link" href="/">返回主站</a></header><section class="login-panel"><div class="eyebrow">Private editing entry</div><h2>后台登录</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">Cloudflare Access 可直接放行；本地预览时使用管理员令牌。</p>${error?`<div class="msg err">${esc(error)}</div>`:""}<form method="post" action="/admin/login"><label>管理员令牌<input type="password" name="token" placeholder="输入 ADMIN_TOKEN"/></label><button class="btn btn-primary" type="submit">进入后台</button></form></section></main>`);
}

function admin(posts: Post[], apps: Application[], flash?: string) {
  const postCards = posts.map((p)=>`<article class="mini"><div class="meta"><span class="pill">${esc(p.status)}</span>${p.pinned?'<span class="pill">Pinned</span>':''}<span>${new Date(p.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(p.title||"未命名动态")}</h3><p>${esc(p.body)}</p><form method="post" action="/admin/posts"><input type="hidden" name="id" value="${esc(p.id)}"/><label>标题<input name="title" value="${esc(p.title)}"/></label><label>Slug<input name="slug" value="${esc(p.slug)}"/></label><label>标签<input name="tags" value="${esc(p.tags.join(", "))}"/></label><label>正文<textarea name="body">${esc(p.body)}</textarea></label><div class="split"><label>状态<select name="status"><option value="draft" ${p.status==="draft"?"selected":""}>draft</option><option value="published" ${p.status==="published"?"selected":""}>published</option></select></label><label>置顶<input class="check" type="checkbox" name="pinned" ${p.pinned?"checked":""}/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form><form method="post" action="/admin/posts/${esc(p.id)}/delete"><button class="btn btn-secondary" type="submit">删除</button></form></article>`).join("");
  const appCards = apps.map((a)=>`<article class="mini"><div class="meta"><span class="pill">${esc(a.status)}</span><span>${new Date(a.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(a.name)}</h3><p><strong>联系：</strong>${esc(a.contact)}</p><p><strong>用途：</strong>${esc(a.requestedSubdomain||"未填写")}</p><p>${esc(a.projectSummary)}</p>${a.notes?`<p><strong>备注：</strong>${esc(a.notes)}</p>`:""}<form class="inline" method="post" action="/admin/applications/${esc(a.id)}/status"><select name="status">${["new","reviewing","accepted","rejected"].map((s)=>`<option value="${s}" ${a.status===s?"selected":""}>${s}</option>`).join("")}</select><button class="btn btn-primary" type="submit">更新状态</button></form></article>`).join("");
  return shell("Admin Dashboard", `<main class="page admin-page"><header class="admin-top"><div><div class="mark">Editing workspace</div><div class="smallcaps">juren233.top / admin</div></div><nav class="toolbar"><a href="/">返回主站</a><form method="post" action="/admin/logout"><button class="ghost-link" type="submit">退出</button></form></nav></header>${flash?`<div class="msg ok">${esc(flash)}</div>`:""}<section class="workspace"><div class="stack"><article class="admin-pane"><div class="eyebrow">Publish short updates</div><h2>发布新动态</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">集中维护上线记录、站点更新和阶段性判断。</p><form method="post" action="/admin/posts"><label>标题<input name="title" placeholder="可选标题，比如上线记录"/></label><label>Slug<input name="slug" placeholder="可留空，系统会自动生成"/></label><label>标签<input name="tags" placeholder="用逗号分隔，例如 brand, 上线, 公告"/></label><label>正文<textarea name="body" placeholder="写下近况、更新、想法或上线播报" required></textarea></label><div class="split"><label>状态<select name="status"><option value="draft">draft</option><option value="published">published</option></select></label><label>置顶显示<input class="check" type="checkbox" name="pinned"/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form></article><article class="admin-pane"><div class="eyebrow">Inbox</div><h2>合作申请</h2><div class="list">${appCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有新的合作申请。</p>'}</div></article></div><article class="admin-pane"><div class="eyebrow">Existing posts</div><h2>已发布与草稿动态</h2><div class="list">${postCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有动态记录。</p>'}</div></article></section></main>`);
}

export function sortPublishedPosts(posts: Post[]): Post[] { return posts.filter((p)=>p.status==="published").sort((a,b)=>pinnedOrder(a,b) || new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()); }
function pinnedOrder(a: Post, b: Post): number { return a.pinned===b.pinned ? 0 : a.pinned ? -1 : 1; }
export function createApplicationInput(input: ApplicationInput): ApplicationInput { const x={name:input.name.trim(),contact:input.contact.trim(),projectSummary:input.projectSummary.trim(),requestedSubdomain:input.requestedSubdomain.trim(),notes:input.notes.trim()}; if(!x.name) throw new Error("申请人名称不能为空"); if(!x.contact) throw new Error("联系方式不能为空"); if(!x.projectSummary) throw new Error("项目说明不能为空"); return x; }
export function verifyAdminAccess(auth:{emailHeader:string|null;cookieToken:string|null}, env:{ADMIN_EMAIL?:string;ADMIN_TOKEN?:string}): boolean { return Boolean((auth.emailHeader&&env.ADMIN_EMAIL&&auth.emailHeader.toLowerCase()===env.ADMIN_EMAIL.toLowerCase())||(auth.cookieToken&&env.ADMIN_TOKEN&&auth.cookieToken===env.ADMIN_TOKEN)); }

function postInput(fd: FormData): PostInput {
  const body=String(fd.get("body")??"").trim();
  if(!body) throw new Error("动态正文不能为空");
  return {
    id:String(fd.get("id")??"").trim()||undefined,
    title:String(fd.get("title")??"").trim(),
    body,
    slug:String(fd.get("slug")??"").trim()||undefined,
    tags:tags(String(fd.get("tags")??"")),
    status:String(fd.get("status")??"draft")==="published"?"published":"draft",
    pinned:["on","true","1"].includes(String(fd.get("pinned")??"")),
  };
}

async function appInput(req: Request): Promise<ApplicationInput> {
  const ct=req.headers.get("content-type")||"";
  if(ct.includes("application/json")) return createApplicationInput(await req.json() as ApplicationInput);
  const f=await req.formData();
  return createApplicationInput({
    name:String(f.get("name")??""),
    contact:String(f.get("contact")??""),
    projectSummary:String(f.get("projectSummary")??""),
    requestedSubdomain:String(f.get("requestedSubdomain")??""),
    notes:String(f.get("notes")??""),
  });
}

const id = (p:string) => `${p}_${crypto.randomUUID()}`;

export function createApp(repo: Repository) {
  const app = new Hono<AppEnv>();
  app.get("/", async (c) => c.html(home(sortPublishedPosts(await repo.listPosts()))));
  app.get("/api/feed", async (c) => c.json({ items: sortPublishedPosts(await repo.listPosts()) }));
  app.post("/api/applications", async (c) => {
    try {
      await repo.createApplication(await appInput(c.req.raw));
      return c.json({ ok:true }, 201);
    } catch (e) {
      return c.json({ error:e instanceof Error?e.message:"提交失败，请稍后重试。" }, 400);
    }
  });
  app.get("/admin/login", (c) => c.html(login()));
  app.post("/admin/login", async (c) => {
    const token=String((await c.req.formData()).get("token")??"");
    if(!c.env.ADMIN_TOKEN || token!==c.env.ADMIN_TOKEN) return c.html(login("令牌不正确，请检查 ADMIN_TOKEN。"), 401);
    setCookie(c,"admin_token",token,{httpOnly:true,sameSite:"Lax",secure:true,path:"/",maxAge:60*60*24*14});
    return c.redirect("/admin");
  });
  app.use("/admin/*", async (c,next) => {
    if(c.req.path==="/admin/login") return next();
    const ok=verifyAdminAccess({ emailHeader:c.req.header("CF-Access-Authenticated-User-Email")??null, cookieToken:getCookie(c,"admin_token")??null }, c.env);
    if(!ok) return c.redirect("/admin/login");
    return next();
  });
  app.get("/admin", async (c) => c.html(admin((await repo.listPosts()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), (await repo.listApplications()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), c.req.query("flash")||undefined)));
  app.post("/admin/posts", async (c) => {
    try {
      await repo.upsertPost(postInput(await c.req.formData()));
      return c.redirect("/admin?flash=动态已保存");
    } catch (e) {
      return c.html(login(e instanceof Error?e.message:"保存失败"), 400);
    }
  });
  app.post("/admin/posts/:id/delete", async (c) => { await repo.deletePost(c.req.param("id")); return c.redirect("/admin?flash=动态已删除"); });
  app.post("/admin/applications/:id/status", async (c) => {
    const status=String((await c.req.formData()).get("status")??"new") as ApplicationStatus;
    await repo.updateApplicationStatus(c.req.param("id"), status);
    return c.redirect("/admin?flash=申请状态已更新");
  });
  app.post("/admin/logout", (c) => { deleteCookie(c,"admin_token",{path:"/"}); return c.redirect("/"); });
  return app;
}

export function createD1Repository(db: D1Database): Repository {
  return {
    async listPosts() {
      const r=await db.prepare("SELECT id,title,body,slug,tags,status,pinned,created_at,updated_at FROM posts ORDER BY datetime(created_at) DESC").all<{id:string;title:string;body:string;slug:string;tags:string;status:PostStatus;pinned:number;created_at:string;updated_at:string}>();
      return (r.results??[]).map((x)=>({ id:x.id,title:x.title,body:x.body,slug:x.slug,tags:x.tags?JSON.parse(x.tags):[],status:x.status,pinned:Boolean(x.pinned),createdAt:x.created_at,updatedAt:x.updated_at }));
    },
    async listApplications() {
      const r=await db.prepare("SELECT id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at FROM applications ORDER BY datetime(created_at) DESC").all<{id:string;name:string;contact:string;project_summary:string;requested_subdomain:string;notes:string;status:ApplicationStatus;created_at:string;updated_at:string}>();
      return (r.results??[]).map((x)=>({ id:x.id,name:x.name,contact:x.contact,projectSummary:x.project_summary,requestedSubdomain:x.requested_subdomain,notes:x.notes,status:x.status,createdAt:x.created_at,updatedAt:x.updated_at }));
    },
    async createApplication(input) {
      const now=new Date().toISOString();
      await db.prepare("INSERT INTO applications (id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, 'new', ?, ?)").bind(id("app"), input.name, input.contact, input.projectSummary, input.requestedSubdomain, input.notes, now, now).run();
    },
    async upsertPost(input) {
      const now=new Date().toISOString(), pid=input.id||id("post"), slug=slugify(input.slug||input.title||pid);
      await db.prepare(`INSERT INTO posts (id,title,body,slug,tags,status,pinned,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, COALESCE((SELECT created_at FROM posts WHERE id = ?), ?), ?) ON CONFLICT(id) DO UPDATE SET title=excluded.title, body=excluded.body, slug=excluded.slug, tags=excluded.tags, status=excluded.status, pinned=excluded.pinned, updated_at=excluded.updated_at`).bind(pid, input.title, input.body, slug, JSON.stringify(input.tags), input.status, input.pinned ? 1 : 0, pid, now, now).run();
    },
    async deletePost(pid) { await db.prepare("DELETE FROM posts WHERE id = ?").bind(pid).run(); },
    async updateApplicationStatus(pid, status) { await db.prepare("UPDATE applications SET status = ?, updated_at = ? WHERE id = ?").bind(status, new Date().toISOString(), pid).run(); },
  };
}
