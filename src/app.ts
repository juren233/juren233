import { deleteCookie, getCookie, setCookie } from "hono/cookie";
import { Hono } from "hono";

export type PostStatus = "draft" | "published";
export type ApplicationStatus = "new" | "reviewing" | "accepted" | "rejected";
export interface Post { id:string; title:string; body:string; slug:string; tags:string[]; status:PostStatus; pinned:boolean; createdAt:string; updatedAt:string; }
export interface Application { id:string; name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; status:ApplicationStatus; createdAt:string; updatedAt:string; }
export interface ApplicationInput { name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; }
export interface PostInput { id?:string; title:string; body:string; slug?:string; tags:string[]; status:PostStatus; pinned:boolean; }
export interface Repository {
  listPosts(): Promise<Post[]>; listApplications(): Promise<Application[]>; createApplication(input: ApplicationInput): Promise<void>;
  upsertPost(input: PostInput): Promise<void>; deletePost(id: string): Promise<void>; updateApplicationStatus(id: string, status: ApplicationStatus): Promise<void>;
}
export interface AppEnv { Bindings: Env; }

const featuredSites = [
  ["share", "share.juren233.top", "https://share.juren233.top", "公开共享与临时投递", true],
  ["pods", "pods.juren233.top", "#cooperate", "实验项目与未来微型服务", false],
  ["mount", "合作挂载", "#cooperate", "为值得展示的内容提供入口", false],
] as const;
const fallbackPosts: Post[] = [{ id:"seed", title:"主站正在成形", body:"这里会持续发布站点更新、上线记录、实验想法与合作动态。它不是传统博客，更像我的品牌流与项目日志。", slug:"seed", tags:["brand","动态"], status:"published", pinned:true, createdAt:"2026-03-22T08:00:00.000Z", updatedAt:"2026-03-22T08:00:00.000Z" }];
const css = `:root{--bg:#f7f1e8;--bg2:#ece1d3;--text:#18120f;--muted:rgba(24,18,15,.62);--line:rgba(24,18,15,.1);--soft:rgba(24,18,15,.06);--paper:rgba(255,251,246,.84);--paper-strong:#fffaf5;--accent:#c45b31;--accent-soft:rgba(196,91,49,.12);--deep:#1c3c37;--ok:#1b5b4e;--err:#8b2d1d;--radius-lg:32px;--radius-md:18px;--radius-sm:14px;--gutter:clamp(18px,4vw,56px);--measure:30rem;--transition:220ms ease}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;font:16px/1.65 "Segoe UI Variable Text","Microsoft YaHei UI",sans-serif;color:var(--text);background:radial-gradient(circle at 12% 10%,rgba(196,91,49,.12),transparent 24%),linear-gradient(180deg,var(--bg),var(--bg2));min-height:100vh}a{text-decoration:none;color:inherit}button,input,textarea,select{font:inherit}button{appearance:none;background:none}img,svg{display:block;max-width:100%}.page{width:100%;padding:22px var(--gutter) 84px}.site-nav,.section-head,.admin-top,.login-shell{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap}.mark,.eyebrow,.smallcaps{font-size:12px;letter-spacing:.24em;text-transform:uppercase;color:var(--muted)}.nav-links,.actions,.meta,.toolbar,.inline,.tags{display:flex;gap:12px;align-items:center;flex-wrap:wrap}.nav-links a,.ghost-link,.toolbar a,.toolbar button{padding:0 0 4px;color:var(--muted);border:0;background:none}.nav-links a:hover,.ghost-link:hover,.toolbar a:hover,.toolbar button:hover{color:var(--text)}.hero{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(320px,.9fr);gap:clamp(26px,5vw,72px);min-height:calc(100svh - 110px);align-items:end;padding:clamp(42px,8vw,110px) 0 clamp(28px,6vw,72px)}.hero-copy{max-width:var(--measure)}.hero-copy h1,.section-head h2,.feature-name,.footer-title,.admin-pane h2,.login-panel h2{font-family:Georgia,"Times New Roman",serif;letter-spacing:-.06em;line-height:.92;margin:0}.hero-copy h1{font-size:clamp(4.4rem,12vw,10rem);margin-top:12px}.hero-copy p{margin:18px 0 0;color:var(--muted);font-size:clamp(1rem,1.45vw,1.12rem)}.actions{margin-top:26px}.btn,button.btn{display:inline-flex;align-items:center;justify-content:center;min-height:48px;padding:0 20px;border-radius:999px;border:0;cursor:pointer;transition:transform var(--transition),background var(--transition),color var(--transition),box-shadow var(--transition)}.btn:hover,button.btn:hover{transform:translateY(-1px)}.btn-primary{background:var(--text);color:#fff9f3}.btn-secondary{box-shadow:inset 0 0 0 1px var(--line);color:var(--text)}.hero-subdomains{margin-top:28px;display:flex;gap:10px;flex-wrap:wrap}.hero-subdomains span{display:inline-flex;min-height:30px;align-items:center;padding:0 12px;border-radius:999px;background:var(--soft);font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.hero-visual{display:grid;align-items:end;min-height:420px}.hero-map{position:relative;min-height:100%;padding:28px 0 0}.hero-map::before{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(255,251,246,.28),rgba(255,251,246,0));pointer-events:none}.domain-grid{position:absolute;inset:auto 0 16px auto;display:grid;gap:10px;justify-items:end}.domain-chip{display:inline-flex;gap:10px;align-items:center;min-height:38px;padding:0 14px;border-radius:999px;background:rgba(255,251,246,.68);backdrop-filter:blur(8px);font-size:13px;color:var(--text);box-shadow:0 10px 30px rgba(24,18,15,.05)}.domain-chip strong{font-weight:600}.brand-map{width:100%;height:auto}.map-node{animation:nodePulse 5.5s ease-in-out infinite}.map-path{stroke-dasharray:6 10;animation:routeDrift 7s linear infinite}.map-draw{stroke-dasharray:1;stroke-dashoffset:1;animation:routeReveal 1.8s ease forwards}.section{padding:clamp(42px,7vw,88px) 0;border-top:1px solid var(--line)}.section-head{margin-bottom:26px}.section-head h2{font-size:clamp(2.4rem,5vw,4.2rem)}.section-head p{max-width:28rem;margin:8px 0 0;color:var(--muted)}.entries{display:grid;gap:18px}.entry-grid{grid-template-columns:1.2fr .9fr .9fr}.entry{display:grid;gap:12px;align-content:start;padding:18px 0;border-top:1px solid var(--line)}.entry:first-child{border-top:0;padding-top:0}.feature-name{font-size:clamp(1.55rem,3vw,2.6rem)}.domain{font-size:12px;letter-spacing:.18em;text-transform:uppercase;color:var(--muted)}.entry p{margin:0;color:var(--muted)}.entry-link{display:inline-flex;align-items:center;gap:8px;font-size:14px}.entry.hot .feature-name,.entry.hot .entry-link{color:var(--accent)}.journal{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px 32px}.journal-item{padding:18px 0;border-top:1px solid var(--line)}.journal-item:nth-child(-n+2){padding-top:0;border-top:0}.journal-item h3{margin:10px 0 8px;font-size:clamp(1.25rem,2.3vw,1.9rem);line-height:1.12}.journal-item p{margin:0;color:var(--muted)}.tag,.pill{display:inline-flex;align-items:center;min-height:28px;padding:0 10px;border-radius:999px;background:var(--soft);font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.cooperate{display:grid;grid-template-columns:minmax(0,.9fr) minmax(320px,.95fr);gap:clamp(24px,4vw,52px);align-items:start}.cooperate-copy{display:grid;gap:18px;max-width:28rem}.editorial-note{padding:18px 0;border-top:1px solid var(--line)}.editorial-note strong{display:block;margin-bottom:6px}.editorial-note p{margin:0;color:var(--muted)}.form-shell,.admin-pane,.login-panel,.footer-block{background:linear-gradient(180deg,rgba(255,251,246,.76),rgba(249,242,233,.62));border-radius:var(--radius-lg);padding:clamp(22px,3vw,34px);box-shadow:0 14px 40px rgba(24,18,15,.05)}form{display:grid;gap:14px}label{display:grid;gap:8px;font-size:14px}input,textarea,select{width:100%;padding:14px 16px;border-radius:var(--radius-sm);border:1px solid var(--line);background:var(--paper-strong);color:var(--text)}textarea{min-height:140px;resize:vertical}.split{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.msg{padding:12px 14px;border-radius:var(--radius-sm);font-size:14px}.ok{background:rgba(27,91,78,.1);color:var(--ok)}.err{background:rgba(139,45,29,.1);color:var(--err)}.page-footer{padding-top:34px}.footer-grid{display:grid;grid-template-columns:minmax(0,.9fr) minmax(0,.72fr);gap:18px}.footer-title{font-size:clamp(2rem,4vw,3rem);margin-bottom:8px}.footer-block p{margin:0;color:var(--muted)}.admin-page,.login-page{min-height:100svh}.admin-top,.login-shell{padding-bottom:34px}.toolbar form{display:block}.workspace{display:grid;grid-template-columns:minmax(320px,.92fr) minmax(0,1.08fr);gap:20px}.stack,.list{display:grid;gap:18px}.admin-pane h2,.login-panel h2{font-size:clamp(2rem,4vw,3rem);margin-top:10px}.mini{padding:18px 0;border-top:1px solid var(--line)}.mini:first-child{padding-top:0;border-top:0}.mini p{margin:0 0 10px}.check{width:20px;height:20px}.login-panel{width:min(100%,520px)}@keyframes routeReveal{to{stroke-dashoffset:0}}@keyframes routeDrift{to{stroke-dashoffset:-64}}@keyframes nodePulse{0%,100%{transform:scale(1);opacity:1}50%{transform:scale(1.08);opacity:.84}}@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}.map-node,.map-path,.map-draw,.btn,button.btn{animation:none;transition:none}}@media (max-width:980px){.hero,.cooperate,.workspace,.journal,.footer-grid,.entry-grid{grid-template-columns:1fr}.hero{min-height:auto}.hero-visual{min-height:360px}.journal-item:nth-child(2){padding-top:18px;border-top:1px solid var(--line)}.domain-grid{position:static;justify-items:start;margin-top:18px}}@media (max-width:720px){.page{padding:20px 18px 72px}.split{grid-template-columns:1fr}.hero-copy h1{font-size:clamp(3.7rem,22vw,5.8rem)}.section-head h2{font-size:clamp(2rem,11vw,3rem)}.nav-links{gap:12px 16px}}`;

const esc = (v:string) => v.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;");
const slugify = (v:string) => v.trim().toLowerCase().replace(/[^a-z0-9\u4e00-\u9fa5\s-]/g,"").replace(/\s+/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"") || `post-${Math.random().toString(36).slice(2,8)}`;
const tags = (v:string|string[]) => (Array.isArray(v)?v:v.split(",")).map((x)=>x.trim()).filter(Boolean).slice(0,6);
const shell = (title:string, body:string) => `<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>${esc(title)}</title><meta name="description" content="juren233.top 的主域名门户，展示精选子域名、合作挂载服务与个人最新动态。"/><style>${css}</style></head><body>${body}</body></html>`;
const brandArtwork = () => `<div class="hero-map"><svg class="brand-map" id="abstract-brand-mark" viewBox="0 0 760 620" role="img" aria-label="domain map for juren233.top"><defs><linearGradient id="route-accent" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#d97a52"/><stop offset="100%" stop-color="#c45b31"/></linearGradient><linearGradient id="route-deep" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#20443e"/><stop offset="100%" stop-color="#122623"/></linearGradient></defs><g fill="none" stroke-linecap="round"><path class="map-path" d="M176 310C250 244 340 208 432 198C538 186 618 226 672 300" stroke="rgba(24,18,15,.16)" stroke-width="2"/><path class="map-path" d="M176 310C246 344 320 368 406 390C506 416 592 412 672 382" stroke="rgba(24,18,15,.16)" stroke-width="2"/><path class="map-draw" d="M176 310C272 252 356 226 448 230" stroke="url(#route-accent)" stroke-width="4"/><path class="map-draw" d="M176 310C280 366 398 398 544 388" stroke="url(#route-deep)" stroke-width="4"/></g><circle cx="176" cy="310" r="84" fill="rgba(255,251,246,.88)" stroke="rgba(24,18,15,.08)"/><circle cx="176" cy="310" r="54" fill="url(#route-accent)" opacity=".9"/><text x="176" y="318" text-anchor="middle" font-family="Georgia, Times New Roman, serif" font-size="28" fill="#fff8f2">main</text><g class="map-node"><circle cx="500" cy="224" r="44" fill="rgba(255,251,246,.92)" stroke="rgba(24,18,15,.08)"/><text x="500" y="231" text-anchor="middle" font-size="18" fill="#18120f">share</text></g><g class="map-node"><circle cx="610" cy="392" r="44" fill="rgba(255,251,246,.92)" stroke="rgba(24,18,15,.08)"/><text x="610" y="399" text-anchor="middle" font-size="18" fill="#18120f">pods</text></g><g><circle cx="684" cy="286" r="16" fill="#1c3c37"/><circle cx="684" cy="338" r="16" fill="#c45b31"/></g></svg><div class="domain-grid"><div class="domain-chip"><strong>share</strong><span>公开共享</span></div><div class="domain-chip"><strong>pods</strong><span>实验服务</span></div><div class="domain-chip"><strong>mount</strong><span>合作入口</span></div></div></div>`;

function home(posts: Post[]) {
  const items = (posts.length ? posts : fallbackPosts).map((p)=>`<article class="journal-item"><div class="meta">${p.pinned?'<span class="pill">Pinned</span>':''}<span>${new Date(p.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(p.title||"未命名动态")}</h3><p>${esc(p.body)}</p><div class="tags">${p.tags.map((t)=>`<span class="tag">${esc(t)}</span>`).join("")}</div></article>`).join("");
  const sites = featuredSites.map(([name,domain,href,desc,hot])=>`<a class="entry ${hot?"hot":""}" href="${href}" ${String(href).startsWith("http")?'target="_blank" rel="noreferrer"':''}><div class="domain">${esc(domain)}</div><h3 class="feature-name">${esc(name)}</h3><p>${esc(desc)}</p><span class="entry-link">${hot?"打开":"查看"}</span></a>`).join("");
  return shell("juren233.top | 主域名门户", `<main class="page"><header class="site-nav"><div><div class="mark">Brand index</div><div class="smallcaps">juren233 / main domain portal</div></div><nav class="nav-links"><a href="#selected">精选入口</a><a href="#journal">动态</a><a href="#cooperate">合作挂载</a><a href="/admin">后台</a></nav></header><section class="hero"><div class="hero-copy"><div class="eyebrow">Main domain</div><h1>juren233.top</h1><p>主域名入口，集中承接精选子域名、合作挂载与最新更新。</p><div class="actions"><a class="btn btn-primary" href="#selected">进入精选入口</a><a class="btn btn-secondary" href="#cooperate">申请挂载</a></div><div class="hero-subdomains"><span>share.juren233.top</span><span>pods.juren233.top</span><span>mount / curated access</span></div></div><div class="hero-visual">${brandArtwork()}</div></section><section class="section" id="selected"><div class="section-head"><div><div class="eyebrow">Selected access</div><h2>精选入口</h2></div><p>当前主要入口与预留方向。</p></div><div class="entries entry-grid">${sites}</div></section><section class="section" id="journal"><div class="section-head"><div><div class="eyebrow">Journal</div><h2>最新动态</h2></div><p>上线、迭代、实验记录。</p></div><div class="journal">${items}</div></section><section class="section" id="cooperate"><div class="section-head"><div><div class="eyebrow">Collaboration</div><h2>合作挂载</h2></div><p>适合独立项目、活动页、作品集与公开资料页。</p></div><div class="cooperate"><div class="cooperate-copy"><div class="editorial-note"><strong>提供内容</strong><p>子域名承接、入口展示、落点建议。</p></div><div class="editorial-note"><strong>处理方式</strong><p>提交后人工查看与沟通。</p></div><div class="editorial-note"><strong>当前状态</strong><p>申请制，手动审核。</p></div></div><div class="form-shell"><form id="cooperation-form"><label>申请人名称<input name="name" placeholder="怎么称呼你" required/></label><label>联系方式<input name="contact" placeholder="邮箱 / QQ / Telegram / 微信" required/></label><label>项目说明<textarea name="projectSummary" placeholder="介绍你的内容、用途和展示方式" required></textarea></label><div class="split"><label>预期子域名 / 用途说明<input name="requestedSubdomain" placeholder="例如 demo、lab、share-guest"/></label><label>备注<input name="notes" placeholder="可填写时效、风格或其他说明"/></label></div><button class="btn btn-primary" type="submit">提交申请</button><div id="form-message" class="mark" style="letter-spacing:.08em;text-transform:none;">申请会直接进入后台列表。</div></form></div></div></section><footer class="page-footer"><div class="footer-grid"><section class="footer-block"><div class="eyebrow">Identity</div><h2 class="footer-title">主域名入口</h2><p>用于承接品牌与子域名访问。</p></section><section class="footer-block"><div class="eyebrow">Contact</div><p>合作挂载优先使用站内申请表单。</p></section></div></footer></main><script>const f=document.getElementById("cooperation-form"),m=document.getElementById("form-message");f?.addEventListener("submit",async(e)=>{e.preventDefault();const d=new FormData(f),p=Object.fromEntries(d.entries());m.textContent="正在提交申请...";try{const r=await fetch("/api/applications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(p)}),j=await r.json();if(!r.ok)throw new Error(j.error||"提交失败");f.reset();m.textContent="申请已提交成功，我会在后台查看并跟进。"}catch(err){m.textContent=err instanceof Error?err.message:"提交失败，请稍后重试。"}});</script>`);
}

function login(error?: string) {
  return shell("Admin Login", `<main class="page login-page"><header class="login-shell"><div><div class="mark">Admin access</div><div class="smallcaps">juren233 / backstage</div></div><a class="ghost-link" href="/">返回主站</a></header><section class="login-panel"><div class="eyebrow">Private editing entry</div><h2>后台登录</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">Cloudflare Access 可直接放行；本地预览时使用管理员令牌。</p>${error?`<div class="msg err">${esc(error)}</div>`:""}<form method="post" action="/admin/login"><label>管理员令牌<input type="password" name="token" placeholder="输入 ADMIN_TOKEN"/></label><button class="btn btn-primary" type="submit">进入后台</button></form></section></main>`);
}

function admin(posts: Post[], apps: Application[], flash?: string) {
  const postCards = posts.map((p)=>`<article class="mini"><div class="meta"><span class="pill">${esc(p.status)}</span>${p.pinned?'<span class="pill">Pinned</span>':''}<span>${new Date(p.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(p.title||"未命名动态")}</h3><p>${esc(p.body)}</p><form method="post" action="/admin/posts"><input type="hidden" name="id" value="${esc(p.id)}"/><label>标题<input name="title" value="${esc(p.title)}"/></label><label>Slug<input name="slug" value="${esc(p.slug)}"/></label><label>标签<input name="tags" value="${esc(p.tags.join(", "))}"/></label><label>正文<textarea name="body">${esc(p.body)}</textarea></label><div class="split"><label>状态<select name="status"><option value="draft" ${p.status==="draft"?"selected":""}>draft</option><option value="published" ${p.status==="published"?"selected":""}>published</option></select></label><label>置顶<input class="check" type="checkbox" name="pinned" ${p.pinned?"checked":""}/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form><form method="post" action="/admin/posts/${esc(p.id)}/delete"><button class="btn btn-secondary" type="submit">删除</button></form></article>`).join("");
  const appCards = apps.map((a)=>`<article class="mini"><div class="meta"><span class="pill">${esc(a.status)}</span><span>${new Date(a.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(a.name)}</h3><p><strong>联系：</strong>${esc(a.contact)}</p><p><strong>用途：</strong>${esc(a.requestedSubdomain||"未填写")}</p><p>${esc(a.projectSummary)}</p>${a.notes?`<p><strong>备注：</strong>${esc(a.notes)}</p>`:""}<form class="inline" method="post" action="/admin/applications/${esc(a.id)}/status"><select name="status">${["new","reviewing","accepted","rejected"].map((s)=>`<option value="${s}" ${a.status===s?"selected":""}>${s}</option>`).join("")}</select><button class="btn btn-primary" type="submit">更新状态</button></form></article>`).join("");
  return shell("Admin Dashboard", `<main class="page admin-page"><header class="admin-top"><div><div class="mark">Editing workspace</div><div class="smallcaps">juren233.top / admin</div></div><nav class="toolbar"><a href="/">返回主站</a><form method="post" action="/admin/logout"><button class="ghost-link" type="submit">退出</button></form></nav></header>${flash?`<div class="msg ok">${esc(flash)}</div>`:""}<section class="workspace"><div class="stack"><article class="admin-pane"><div class="eyebrow">Publish short updates</div><h2>发布新动态</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">集中维护上线记录、站点更新和阶段性判断。</p><form method="post" action="/admin/posts"><label>标题<input name="title" placeholder="可选标题，比如 share 上线记录"/></label><label>Slug<input name="slug" placeholder="可留空，系统会自动生成"/></label><label>标签<input name="tags" placeholder="用逗号分隔，例如 share, 上线, 品牌"/></label><label>正文<textarea name="body" placeholder="写下近况、更新、想法或上线播报" required></textarea></label><div class="split"><label>状态<select name="status"><option value="draft">draft</option><option value="published">published</option></select></label><label>置顶显示<input class="check" type="checkbox" name="pinned"/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form></article><article class="admin-pane"><div class="eyebrow">Inbox</div><h2>合作申请</h2><div class="list">${appCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有新的合作申请。</p>'}</div></article></div><article class="admin-pane"><div class="eyebrow">Existing posts</div><h2>已发布与草稿动态</h2><div class="list">${postCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有动态记录。</p>'}</div></article></section></main>`);
}

export function sortPublishedPosts(posts: Post[]): Post[] { return posts.filter((p)=>p.status==="published").sort((a,b)=>a.pinned===b.pinned?new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime():a.pinned?-1:1); }
export function createApplicationInput(input: ApplicationInput): ApplicationInput { const x={name:input.name.trim(),contact:input.contact.trim(),projectSummary:input.projectSummary.trim(),requestedSubdomain:input.requestedSubdomain.trim(),notes:input.notes.trim()}; if(!x.name) throw new Error("申请人名称不能为空"); if(!x.contact) throw new Error("联系方式不能为空"); if(!x.projectSummary) throw new Error("项目说明不能为空"); return x; }
export function verifyAdminAccess(auth:{emailHeader:string|null;cookieToken:string|null}, env:{ADMIN_EMAIL?:string;ADMIN_TOKEN?:string}): boolean { return Boolean((auth.emailHeader&&env.ADMIN_EMAIL&&auth.emailHeader.toLowerCase()===env.ADMIN_EMAIL.toLowerCase())||(auth.cookieToken&&env.ADMIN_TOKEN&&auth.cookieToken===env.ADMIN_TOKEN)); }

function postInput(fd: FormData): PostInput { const body=String(fd.get("body")??"").trim(); if(!body) throw new Error("动态正文不能为空"); return { id:String(fd.get("id")??"").trim()||undefined, title:String(fd.get("title")??"").trim(), body, slug:String(fd.get("slug")??"").trim()||undefined, tags:tags(String(fd.get("tags")??"")), status:String(fd.get("status")??"draft")==="published"?"published":"draft", pinned:["on","true","1"].includes(String(fd.get("pinned")??"")) }; }
async function appInput(req: Request): Promise<ApplicationInput> { const ct=req.headers.get("content-type")||""; if(ct.includes("application/json")) return createApplicationInput(await req.json() as ApplicationInput); const f=await req.formData(); return createApplicationInput({ name:String(f.get("name")??""), contact:String(f.get("contact")??""), projectSummary:String(f.get("projectSummary")??""), requestedSubdomain:String(f.get("requestedSubdomain")??""), notes:String(f.get("notes")??"") }); }
const id = (p:string) => `${p}_${crypto.randomUUID()}`;

export function createApp(repo: Repository) {
  const app = new Hono<AppEnv>();
  app.get("/", async (c) => c.html(home(sortPublishedPosts(await repo.listPosts()))));
  app.get("/api/feed", async (c) => c.json({ items: sortPublishedPosts(await repo.listPosts()) }));
  app.post("/api/applications", async (c) => { try { await repo.createApplication(await appInput(c.req.raw)); return c.json({ ok:true }, 201); } catch (e) { return c.json({ error:e instanceof Error?e.message:"提交失败，请稍后重试。" }, 400); } });
  app.get("/admin/login", (c) => c.html(login()));
  app.post("/admin/login", async (c) => { const token=String((await c.req.formData()).get("token")??""); if(!c.env.ADMIN_TOKEN || token!==c.env.ADMIN_TOKEN) return c.html(login("令牌不正确，请检查 ADMIN_TOKEN。"), 401); setCookie(c,"admin_token",token,{httpOnly:true,sameSite:"Lax",secure:true,path:"/",maxAge:60*60*24*14}); return c.redirect("/admin"); });
  app.use("/admin/*", async (c,next) => { if(c.req.path==="/admin/login") return next(); const ok=verifyAdminAccess({ emailHeader:c.req.header("CF-Access-Authenticated-User-Email")??null, cookieToken:getCookie(c,"admin_token")??null }, c.env); if(!ok) return c.redirect("/admin/login"); return next(); });
  app.get("/admin", async (c) => c.html(admin((await repo.listPosts()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), (await repo.listApplications()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), c.req.query("flash")||undefined)));
  app.post("/admin/posts", async (c) => { try { await repo.upsertPost(postInput(await c.req.formData())); return c.redirect("/admin?flash=动态已保存"); } catch (e) { return c.html(login(e instanceof Error?e.message:"保存失败"), 400); } });
  app.post("/admin/posts/:id/delete", async (c) => { await repo.deletePost(c.req.param("id")); return c.redirect("/admin?flash=动态已删除"); });
  app.post("/admin/applications/:id/status", async (c) => { const status=String((await c.req.formData()).get("status")??"new") as ApplicationStatus; await repo.updateApplicationStatus(c.req.param("id"), status); return c.redirect("/admin?flash=申请状态已更新"); });
  app.post("/admin/logout", (c) => { deleteCookie(c,"admin_token",{path:"/"}); return c.redirect("/"); });
  return app;
}

export function createD1Repository(db: D1Database): Repository {
  return {
    async listPosts() { const r=await db.prepare("SELECT id,title,body,slug,tags,status,pinned,created_at,updated_at FROM posts ORDER BY datetime(created_at) DESC").all<{id:string;title:string;body:string;slug:string;tags:string;status:PostStatus;pinned:number;created_at:string;updated_at:string}>(); return (r.results??[]).map((x)=>({ id:x.id,title:x.title,body:x.body,slug:x.slug,tags:x.tags?JSON.parse(x.tags):[],status:x.status,pinned:Boolean(x.pinned),createdAt:x.created_at,updatedAt:x.updated_at })); },
    async listApplications() { const r=await db.prepare("SELECT id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at FROM applications ORDER BY datetime(created_at) DESC").all<{id:string;name:string;contact:string;project_summary:string;requested_subdomain:string;notes:string;status:ApplicationStatus;created_at:string;updated_at:string}>(); return (r.results??[]).map((x)=>({ id:x.id,name:x.name,contact:x.contact,projectSummary:x.project_summary,requestedSubdomain:x.requested_subdomain,notes:x.notes,status:x.status,createdAt:x.created_at,updatedAt:x.updated_at })); },
    async createApplication(input) { const now=new Date().toISOString(); await db.prepare("INSERT INTO applications (id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, 'new', ?, ?)").bind(id("app"), input.name, input.contact, input.projectSummary, input.requestedSubdomain, input.notes, now, now).run(); },
    async upsertPost(input) { const now=new Date().toISOString(), pid=input.id||id("post"), slug=slugify(input.slug||input.title||pid); await db.prepare(`INSERT INTO posts (id,title,body,slug,tags,status,pinned,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, COALESCE((SELECT created_at FROM posts WHERE id = ?), ?), ?) ON CONFLICT(id) DO UPDATE SET title=excluded.title, body=excluded.body, slug=excluded.slug, tags=excluded.tags, status=excluded.status, pinned=excluded.pinned, updated_at=excluded.updated_at`).bind(pid, input.title, input.body, slug, JSON.stringify(input.tags), input.status, input.pinned ? 1 : 0, pid, now, now).run(); },
    async deletePost(pid) { await db.prepare("DELETE FROM posts WHERE id = ?").bind(pid).run(); },
    async updateApplicationStatus(pid, status) { await db.prepare("UPDATE applications SET status = ?, updated_at = ? WHERE id = ?").bind(status, new Date().toISOString(), pid).run(); },
  };
}
