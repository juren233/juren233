import { deleteCookie, getCookie, setCookie } from "hono/cookie";
import { Hono } from "hono";

export type PostStatus = "draft" | "published";
export type ApplicationStatus = "new" | "reviewing" | "accepted" | "rejected";
export interface Post { id:string; title:string; body:string; slug:string; tags:string[]; status:PostStatus; pinned:boolean; createdAt:string; updatedAt:string; }
export interface Application { id:string; name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; status:ApplicationStatus; createdAt:string; updatedAt:string; }
export interface ApplicationInput { name:string; contact:string; projectSummary:string; requestedSubdomain:string; notes:string; }
export interface PostInput { id?:string; title:string; body:string; slug?:string; tags:string[]; status:PostStatus; pinned:boolean; }
export interface Repository {
  listPosts(): Promise<Post[]>; listApplications(): Promise<Application[]>; createApplication(input: ApplicationInput): Promise<void>;
  upsertPost(input: PostInput): Promise<void>; deletePost(id: string): Promise<void>; updateApplicationStatus(id: string, status: ApplicationStatus): Promise<void>;
}
export interface AppEnv { Bindings: Env; }

const fallbackPosts: Post[] = [{ id:"seed", title:"主站正在成形", body:"这里会持续发布站点更新、上线记录、实验想法与合作动态。它不是传统博客，更像我的品牌流与项目日志。", slug:"seed", tags:["brand","动态"], status:"published", pinned:true, createdAt:"2026-03-22T08:00:00.000Z", updatedAt:"2026-03-22T08:00:00.000Z" }];
const css = `:root{--bg:#f4fbf6;--bg-soft:#eef8f1;--bg-deep:#e1f1e6;--mint:#b9ef9a;--mint-soft:#dff7d2;--mint-line:rgba(143,204,118,.36);--line:rgba(14,36,21,.1);--text:#102018;--muted:rgba(16,32,24,.62);--shadow:0 30px 90px rgba(55,116,77,.08);--radius-xl:38px;--radius-lg:30px;--radius-sm:14px;--gutter:clamp(18px,4vw,56px);--content:1280px;--transition:280ms cubic-bezier(.22,1,.36,1);--hero-shift:0px;--hero-tilt:0deg;--glow-shift:0px}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;font:16px/1.6 "Segoe UI Variable Text","PingFang SC","Microsoft YaHei UI",sans-serif;color:var(--text);background:radial-gradient(circle at 14% 10%,rgba(255,255,255,.94),transparent 0 28%),radial-gradient(circle at 78% 18%,rgba(198,244,173,.52),transparent 0 22%),radial-gradient(circle at 64% 58%,rgba(228,251,214,.72),transparent 0 26%),linear-gradient(180deg,var(--bg) 0%,var(--bg-soft) 42%,var(--bg-deep) 100%);min-height:100vh;overflow-x:hidden}a{text-decoration:none;color:inherit}button,input,textarea,select{font:inherit}button{appearance:none;background:none}img,svg{display:block;max-width:100%}.page{position:relative;width:min(100%,var(--content));margin:0 auto;padding:24px var(--gutter) 92px}.page::before,.page::after{content:"";position:fixed;pointer-events:none;z-index:-1;filter:blur(100px);opacity:.92}.page::before{top:-90px;left:-30px;width:360px;height:360px;background:rgba(255,255,255,.88)}.page::after{right:-120px;top:22vh;width:420px;height:420px;background:rgba(188,239,158,.42)}.site-nav,.section-head,.admin-top,.login-shell{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap}.site-nav{position:sticky;top:16px;z-index:20;padding:14px 18px;border:1px solid rgba(255,255,255,.8);background:rgba(255,255,255,.45);backdrop-filter:blur(28px) saturate(140%);border-radius:999px;box-shadow:0 12px 40px rgba(65,120,82,.06)}.mark,.eyebrow,.smallcaps{font-size:11px;letter-spacing:.22em;text-transform:uppercase;color:var(--muted)}.nav-links,.actions,.meta,.toolbar,.inline,.tags{display:flex;gap:12px;align-items:center;flex-wrap:wrap}.nav-links a,.nav-links button,.ghost-link,.toolbar a,.toolbar button{padding:6px 0;color:var(--muted);border:0;background:none;transition:color var(--transition),opacity var(--transition)}.nav-links a:hover,.nav-links button:hover,.ghost-link:hover,.toolbar a:hover,.toolbar button:hover{color:var(--text)}.hero{position:relative;display:grid;grid-template-columns:minmax(0,.9fr) minmax(380px,.95fr);gap:clamp(28px,5vw,90px);align-items:end;min-height:calc(100svh - 126px);padding:clamp(34px,6vw,84px) 0 36px}.hero::before{content:"";position:absolute;inset:8% -8% auto 42%;height:46%;background:radial-gradient(circle,rgba(198,244,173,.56),rgba(198,244,173,0) 70%);transform:translateY(calc(var(--glow-shift) * -1));filter:blur(34px);pointer-events:none}.hero-copy,.hero-visual,.journal-lead,.footer-block,.admin-pane,.login-panel{will-change:transform,opacity}.hero-copy{max-width:32rem;transform:translateY(calc(var(--hero-shift) * -.34))}.hero-copy h1,.section-head h2,.footer-title,.admin-pane h2,.login-panel h2,.modal-panel h2{font-family:Georgia,"Times New Roman",serif;letter-spacing:-.07em;line-height:.9;margin:0}.hero-copy h1{font-size:clamp(4.8rem,13vw,10rem);margin-top:12px}.hero-copy p{margin:16px 0 0;max-width:18rem;color:var(--muted);font-size:clamp(1rem,1.3vw,1.08rem)}.actions{margin-top:28px}.btn,button.btn{display:inline-flex;align-items:center;justify-content:center;min-height:52px;padding:0 22px;border-radius:999px;border:0;cursor:pointer;transition:transform var(--transition),background var(--transition),color var(--transition),box-shadow var(--transition),opacity var(--transition)}.btn:hover,button.btn:hover{transform:translateY(-1px)}.btn-primary{background:#152219;color:#f6fff5;box-shadow:0 20px 44px rgba(21,34,25,.16)}.btn-secondary{background:rgba(255,255,255,.54);backdrop-filter:blur(18px);box-shadow:inset 0 0 0 1px rgba(255,255,255,.72)}.hero-meta{margin-top:26px;display:flex;gap:10px;flex-wrap:wrap}.hero-meta span,.pill,.tag{display:inline-flex;align-items:center;min-height:30px;padding:0 12px;border-radius:999px;background:rgba(255,255,255,.5);backdrop-filter:blur(14px);box-shadow:inset 0 0 0 1px rgba(255,255,255,.72);font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.hero-visual{position:relative;min-height:560px;transform:translateY(calc(var(--hero-shift) * .56)) rotate(var(--hero-tilt))}.hero-stage{position:absolute;inset:0;overflow:hidden}.hero-stage::before{content:"";position:absolute;inset:0;background:linear-gradient(135deg,rgba(255,255,255,.58),transparent 42%,rgba(255,255,255,.2));pointer-events:none}.orb{position:absolute;border-radius:50%;filter:blur(20px);opacity:.92}.orb-a{width:200px;height:200px;top:12%;left:4%;background:rgba(255,255,255,.82);transform:translate3d(0,calc(var(--hero-shift) * -.4),0)}.orb-b{width:300px;height:300px;right:3%;top:8%;background:rgba(193,246,165,.62);transform:translate3d(0,calc(var(--hero-shift) * .3),0)}.orb-c{width:260px;height:260px;left:26%;bottom:0;background:rgba(228,251,214,.88);transform:translate3d(0,calc(var(--hero-shift) * .18),0)}.hero-grid{position:absolute;inset:0;display:grid;place-items:center;padding:10px}.hero-plane{position:relative;width:min(100%,640px);aspect-ratio:.92;border-radius:44px;background:linear-gradient(180deg,rgba(255,255,255,.76),rgba(240,255,239,.24));border:1px solid rgba(255,255,255,.74);box-shadow:var(--shadow);backdrop-filter:blur(28px) saturate(145%);overflow:hidden}.hero-plane::before{content:"";position:absolute;inset:-10% 28% 48% -2%;background:linear-gradient(135deg,rgba(255,255,255,.88),rgba(255,255,255,0));transform:rotate(-14deg)}.poster-frame{position:absolute;inset:18px;border-radius:34px;border:1px solid rgba(255,255,255,.8)}.poster-core,.poster-core::before,.poster-core::after{position:absolute;border-radius:52% 48% 58% 42%/46% 54% 46% 54%}.poster-core{inset:18% 16% 18% 20%;background:linear-gradient(145deg,rgba(255,255,255,.8),rgba(202,246,175,.76) 34%,rgba(165,222,133,.68) 58%,rgba(241,255,240,.54));box-shadow:inset 0 0 0 1px rgba(255,255,255,.64),0 24px 60px rgba(109,171,98,.18)}.poster-core::before{content:"";inset:10% 36% 22% 6%;background:linear-gradient(150deg,rgba(255,255,255,.8),rgba(225,255,211,.42));filter:blur(2px);transform:rotate(-16deg)}.poster-core::after{content:"";inset:30% 8% 8% 34%;background:linear-gradient(160deg,rgba(147,214,118,.54),rgba(255,255,255,.22));filter:blur(8px);transform:rotate(14deg)}.poster-ribbon,.poster-ribbon::before,.poster-ribbon::after{position:absolute;content:"";display:block;border-radius:999px;background:linear-gradient(90deg,rgba(255,255,255,.12),rgba(255,255,255,.74),rgba(255,255,255,.12))}.poster-ribbon{height:1px;left:8%;right:8%;top:24%}.poster-ribbon::before{height:1px;left:8%;right:8%;top:132px}.poster-ribbon::after{height:1px;left:16%;right:16%;top:254px}.poster-caption{position:absolute;left:24px;right:24px;bottom:24px;display:flex;justify-content:space-between;gap:16px;align-items:end}.poster-caption strong{font-size:clamp(1.6rem,2vw,2.3rem);font-weight:600}.poster-caption span{display:block;color:var(--muted);font-size:13px}.section{padding:clamp(42px,7vw,88px) 0;border-top:1px solid var(--line)}.section[data-reveal]{opacity:.01;transform:translateY(34px);transition:transform 840ms cubic-bezier(.22,1,.36,1),opacity 840ms cubic-bezier(.22,1,.36,1)}.section.is-visible[data-reveal]{opacity:1;transform:none}.section-head{margin-bottom:28px}.section-head h2{font-size:clamp(2.3rem,4.8vw,4.2rem)}.section-head p{max-width:22rem;margin:8px 0 0;color:var(--muted)}.journal-stage{display:grid;grid-template-columns:minmax(0,1fr) minmax(220px,.32fr);gap:28px;align-items:end}.journal-lead{padding:32px;border-radius:var(--radius-xl);background:linear-gradient(180deg,rgba(255,255,255,.72),rgba(234,250,228,.5));border:1px solid rgba(255,255,255,.82);box-shadow:var(--shadow);backdrop-filter:blur(26px) saturate(140%)}.journal-lead h3{margin:14px 0 10px;font-size:clamp(1.8rem,3vw,3rem);line-height:.98}.journal-lead p{margin:0;max-width:40rem;color:var(--muted);font-size:clamp(1rem,1.28vw,1.1rem)}.journal-aside{display:grid;gap:10px;align-content:end}.journal-aside .eyebrow{color:rgba(16,32,24,.48)}.journal-date{font-family:Georgia,"Times New Roman",serif;font-size:clamp(2rem,4vw,3.4rem);letter-spacing:-.08em;line-height:.9}.cooperate-cta{display:grid;gap:16px;justify-items:start;padding:30px 0 0;border-top:1px solid var(--line)}.cooperate-cta p{max-width:20rem;margin:0;color:var(--muted)}.page-footer{padding-top:28px}.footer-grid{display:grid;grid-template-columns:minmax(0,.9fr) minmax(0,.7fr);gap:18px}.footer-block,.form-shell,.admin-pane,.login-panel,.modal-panel{background:linear-gradient(180deg,rgba(255,255,255,.68),rgba(242,255,241,.38));border:1px solid rgba(255,255,255,.8);border-radius:var(--radius-xl);padding:clamp(22px,3vw,34px);box-shadow:var(--shadow);backdrop-filter:blur(28px) saturate(140%)}.footer-title{font-size:clamp(2rem,4vw,3rem);margin-bottom:8px}.footer-block p,.admin-pane p{margin:0;color:var(--muted)}form{display:grid;gap:14px}label{display:grid;gap:8px;font-size:14px}input,textarea,select{width:100%;padding:14px 16px;border-radius:var(--radius-sm);border:1px solid rgba(120,177,106,.18);background:rgba(255,255,255,.84);color:var(--text)}textarea{min-height:140px;resize:vertical}.split{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.msg{padding:12px 14px;border-radius:var(--radius-sm);font-size:14px}.ok{background:rgba(27,91,78,.1);color:#1b5b4e}.err{background:rgba(139,45,29,.1);color:#8b2d1d}.modal{position:fixed;inset:0;display:grid;place-items:center;padding:20px;opacity:0;visibility:hidden;pointer-events:none;transition:opacity var(--transition),visibility var(--transition);z-index:60}.modal.is-open{opacity:1;visibility:visible;pointer-events:auto}.modal-backdrop{position:absolute;inset:0;background:rgba(239,248,241,.54);backdrop-filter:blur(24px)}.modal-panel{position:relative;width:min(100%,760px);max-height:min(88vh,920px);overflow:auto}.modal-head{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;margin-bottom:18px}.modal-head p{margin:10px 0 0;color:var(--muted)}.icon-btn{width:42px;height:42px;border-radius:999px;background:rgba(255,255,255,.64);box-shadow:inset 0 0 0 1px rgba(255,255,255,.72)}.admin-page,.login-page{min-height:100svh}.admin-top,.login-shell{padding-bottom:34px}.toolbar form{display:block}.workspace{display:grid;grid-template-columns:minmax(320px,.92fr) minmax(0,1.08fr);gap:20px}.stack,.list{display:grid;gap:18px}.admin-pane h2,.login-panel h2{font-size:clamp(2rem,4vw,3rem);margin-top:10px}.mini{padding:18px 0;border-top:1px solid var(--line)}.mini:first-child{padding-top:0;border-top:0}.mini p{margin:0 0 10px}.check{width:20px;height:20px}.login-panel{width:min(100%,520px)}@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}*,*::before,*::after{animation:none!important;transition:none!important}}@media (max-width:980px){.hero,.workspace,.footer-grid,.journal-stage{grid-template-columns:1fr}.hero{min-height:auto;padding-top:42px}.hero-visual{min-height:460px}}@media (max-width:720px){.page{padding:18px 18px 76px}.site-nav{top:12px;padding:14px 16px;border-radius:28px}.hero-copy h1{font-size:clamp(3.8rem,22vw,5.8rem)}.section-head h2{font-size:clamp(2rem,11vw,3rem)}.split{grid-template-columns:1fr}.hero-visual{min-height:360px}.hero-plane{border-radius:28px}.poster-caption{left:18px;right:18px;bottom:18px}.modal-panel{padding:22px}}`;

const esc = (v:string) => v.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;");
const slugify = (v:string) => v.trim().toLowerCase().replace(/[^a-z0-9\u4e00-\u9fa5\s-]/g,"").replace(/\s+/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"") || `post-${Math.random().toString(36).slice(2,8)}`;
const tags = (v:string|string[]) => (Array.isArray(v)?v:v.split(",")).map((x)=>x.trim()).filter(Boolean).slice(0,6);
const shell = (title:string, body:string) => `<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>${esc(title)}</title><meta name="description" content="juren233.top 的主域名门户，展示精选子域名、合作挂载服务与个人最新动态。"/><style>${css}</style></head><body>${body}</body></html>`;
const brandArtwork = () => `<div class="hero-stage" aria-hidden="true"><div class="orb orb-a"></div><div class="orb orb-b"></div><div class="orb orb-c"></div><div class="hero-grid"><div class="hero-plane" id="abstract-brand-mark"><div class="poster-frame"></div><div class="poster-ribbon"></div><div class="poster-core"></div><div class="poster-caption"><div><strong>juren233</strong><span>poster edition</span></div><span>2026</span></div></div></div></div>`;

function home(posts: Post[]) {
  const lead = (posts.length ? posts : fallbackPosts)[0];
  const leadHtml = `<article class="journal-lead"><div class="meta">${lead.pinned?'<span class="pill">Pinned</span>':''}<span>${new Date(lead.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(lead.title||"未命名动态")}</h3><p>${esc(lead.body)}</p><div class="tags">${lead.tags.map((t)=>`<span class="tag">${esc(t)}</span>`).join("")}</div></article>`;
  return shell("juren233.top | 主域名门户", `<main class="page"><header class="site-nav"><div><div class="mark">Main portal</div><div class="smallcaps">juren233</div></div><nav class="nav-links"><a href="#journal">动态</a><button type="button" data-open-modal="cooperation-modal">合作申请</button><a href="/admin">后台</a></nav></header><section class="hero"><div class="hero-copy"><div class="eyebrow">Main domain</div><h1>juren233.top</h1><p>海报式首页，只留下最必要的更新与入口。</p><div class="actions"><button class="btn btn-primary" type="button" data-open-modal="cooperation-modal">打开合作申请</button></div><div class="hero-meta"><span>apple green tone</span><span>poster landing</span></div></div><div class="hero-visual">${brandArtwork()}</div></section><section class="section" id="journal" data-reveal><div class="section-head"><div><div class="eyebrow">Journal</div><h2>最新动态</h2></div><p>首页只展示最新一条记录。</p></div><div class="journal-stage">${leadHtml}<aside class="journal-aside"><div class="eyebrow">Latest entry</div><div class="journal-date">${new Date(lead.createdAt).toLocaleDateString("zh-CN")}</div><div class="mark" style="letter-spacing:.08em;text-transform:none;">保持克制，只保留一条。</div></aside></div></section><section class="section" id="cooperate" data-reveal><div class="cooperate-cta"><div><div class="eyebrow">Collaboration</div><h2 class="footer-title" style="margin-top:10px;">合作申请</h2></div><p>通过一张冷白玻璃信息牌承接合作表单。</p><button class="btn btn-primary" type="button" data-open-modal="cooperation-modal">打开合作申请</button></div></section><footer class="page-footer"><div class="footer-grid"><section class="footer-block"><div class="eyebrow">Identity</div><h2 class="footer-title">juren233.top</h2><p>主站入口与更新公告。</p></section><section class="footer-block"><div class="eyebrow">Contact</div><p>合作申请统一通过弹窗表单提交。</p></section></div></footer></main><div class="modal" id="cooperation-modal" aria-hidden="true"><div class="modal-backdrop" data-close-modal></div><section class="modal-panel" role="dialog" aria-modal="true" aria-labelledby="cooperation-title"><div class="modal-head"><div><div class="eyebrow">Collaboration request</div><h2 id="cooperation-title">合作申请</h2><p>提交后会直接进入后台列表。</p></div><button class="icon-btn" type="button" aria-label="关闭合作申请" data-close-modal>×</button></div><div class="form-shell" style="padding:0;background:none;border:0;box-shadow:none;backdrop-filter:none;"><form id="cooperation-form"><label>申请人名称<input name="name" placeholder="怎么称呼你" required/></label><label>联系方式<input name="contact" placeholder="邮箱 / QQ / Telegram / 微信" required/></label><label>项目说明<textarea name="projectSummary" placeholder="介绍你的内容、用途和展示方式" required></textarea></label><div class="split"><label>预期子域名 / 用途说明<input name="requestedSubdomain" placeholder="例如 demo、lab、share-guest"/></label><label>备注<input name="notes" placeholder="可填写时效、风格或其他说明"/></label></div><button class="btn btn-primary" type="submit">提交申请</button><div id="form-message" class="mark" style="letter-spacing:.08em;text-transform:none;">申请会直接进入后台列表。</div></form></div></section></div><script>const root=document.documentElement;const modal=document.getElementById("cooperation-modal");const openers=document.querySelectorAll('[data-open-modal="cooperation-modal"]');const closers=document.querySelectorAll("[data-close-modal]");const form=document.getElementById("cooperation-form");const message=document.getElementById("form-message");const prefersReduced=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const setModal=(open)=>{if(!modal)return;modal.classList.toggle("is-open",open);modal.setAttribute("aria-hidden",String(!open));document.body.style.overflow=open?"hidden":"";};openers.forEach((node)=>node.addEventListener("click",()=>setModal(true)));closers.forEach((node)=>node.addEventListener("click",()=>setModal(false)));window.addEventListener("keydown",(event)=>{if(event.key==="Escape")setModal(false);});if(!prefersReduced){const onScroll=()=>{const progress=Math.min(window.scrollY/900,1);root.style.setProperty("--hero-shift",String(progress*46)+"px");root.style.setProperty("--hero-tilt",String(progress*-2.6)+"deg");root.style.setProperty("--glow-shift",String(progress*32)+"px");};onScroll();window.addEventListener("scroll",onScroll,{passive:true});const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting)entry.target.classList.add("is-visible");});},{threshold:.18});document.querySelectorAll("[data-reveal]").forEach((node)=>observer.observe(node));}else{document.querySelectorAll("[data-reveal]").forEach((node)=>node.classList.add("is-visible"));}form?.addEventListener("submit",async(event)=>{event.preventDefault();if(!message)return;const data=new FormData(form);const payload=Object.fromEntries(data.entries());message.textContent="正在提交申请...";try{const response=await fetch("/api/applications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const json=await response.json();if(!response.ok)throw new Error(json.error||"提交失败");form.reset();message.textContent="申请已提交成功，我会在后台查看并跟进。";}catch(error){message.textContent=error instanceof Error?error.message:"提交失败，请稍后重试。";}});</script>`);
}

function login(error?: string) {
  return shell("Admin Login", `<main class="page login-page"><header class="login-shell"><div><div class="mark">Admin access</div><div class="smallcaps">juren233 / backstage</div></div><a class="ghost-link" href="/">返回主站</a></header><section class="login-panel"><div class="eyebrow">Private editing entry</div><h2>后台登录</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">Cloudflare Access 可直接放行；本地预览时使用管理员令牌。</p>${error?`<div class="msg err">${esc(error)}</div>`:""}<form method="post" action="/admin/login"><label>管理员令牌<input type="password" name="token" placeholder="输入 ADMIN_TOKEN"/></label><button class="btn btn-primary" type="submit">进入后台</button></form></section></main>`);
}

function admin(posts: Post[], apps: Application[], flash?: string) {
  const postCards = posts.map((p)=>`<article class="mini"><div class="meta"><span class="pill">${esc(p.status)}</span>${p.pinned?'<span class="pill">Pinned</span>':''}<span>${new Date(p.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(p.title||"未命名动态")}</h3><p>${esc(p.body)}</p><form method="post" action="/admin/posts"><input type="hidden" name="id" value="${esc(p.id)}"/><label>标题<input name="title" value="${esc(p.title)}"/></label><label>Slug<input name="slug" value="${esc(p.slug)}"/></label><label>标签<input name="tags" value="${esc(p.tags.join(", "))}"/></label><label>正文<textarea name="body">${esc(p.body)}</textarea></label><div class="split"><label>状态<select name="status"><option value="draft" ${p.status==="draft"?"selected":""}>draft</option><option value="published" ${p.status==="published"?"selected":""}>published</option></select></label><label>置顶<input class="check" type="checkbox" name="pinned" ${p.pinned?"checked":""}/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form><form method="post" action="/admin/posts/${esc(p.id)}/delete"><button class="btn btn-secondary" type="submit">删除</button></form></article>`).join("");
  const appCards = apps.map((a)=>`<article class="mini"><div class="meta"><span class="pill">${esc(a.status)}</span><span>${new Date(a.createdAt).toLocaleDateString("zh-CN")}</span></div><h3>${esc(a.name)}</h3><p><strong>联系：</strong>${esc(a.contact)}</p><p><strong>用途：</strong>${esc(a.requestedSubdomain||"未填写")}</p><p>${esc(a.projectSummary)}</p>${a.notes?`<p><strong>备注：</strong>${esc(a.notes)}</p>`:""}<form class="inline" method="post" action="/admin/applications/${esc(a.id)}/status"><select name="status">${["new","reviewing","accepted","rejected"].map((s)=>`<option value="${s}" ${a.status===s?"selected":""}>${s}</option>`).join("")}</select><button class="btn btn-primary" type="submit">更新状态</button></form></article>`).join("");
  return shell("Admin Dashboard", `<main class="page admin-page"><header class="admin-top"><div><div class="mark">Editing workspace</div><div class="smallcaps">juren233.top / admin</div></div><nav class="toolbar"><a href="/">返回主站</a><form method="post" action="/admin/logout"><button class="ghost-link" type="submit">退出</button></form></nav></header>${flash?`<div class="msg ok">${esc(flash)}</div>`:""}<section class="workspace"><div class="stack"><article class="admin-pane"><div class="eyebrow">Publish short updates</div><h2>发布新动态</h2><p class="mark" style="letter-spacing:.08em;text-transform:none;">集中维护上线记录、站点更新和阶段性判断。</p><form method="post" action="/admin/posts"><label>标题<input name="title" placeholder="可选标题，比如 share 上线记录"/></label><label>Slug<input name="slug" placeholder="可留空，系统会自动生成"/></label><label>标签<input name="tags" placeholder="用逗号分隔，例如 share, 上线, 品牌"/></label><label>正文<textarea name="body" placeholder="写下近况、更新、想法或上线播报" required></textarea></label><div class="split"><label>状态<select name="status"><option value="draft">draft</option><option value="published">published</option></select></label><label>置顶显示<input class="check" type="checkbox" name="pinned"/></label></div><button class="btn btn-primary" type="submit">保存动态</button></form></article><article class="admin-pane"><div class="eyebrow">Inbox</div><h2>合作申请</h2><div class="list">${appCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有新的合作申请。</p>'}</div></article></div><article class="admin-pane"><div class="eyebrow">Existing posts</div><h2>已发布与草稿动态</h2><div class="list">${postCards||'<p class="mark" style="letter-spacing:.08em;text-transform:none;">目前还没有动态记录。</p>'}</div></article></section></main>`);
}

export function sortPublishedPosts(posts: Post[]): Post[] { return posts.filter((p)=>p.status==="published").sort((a,b)=>a.pinned===b.pinned?new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime():a.pinned?-1:1); }
export function createApplicationInput(input: ApplicationInput): ApplicationInput { const x={name:input.name.trim(),contact:input.contact.trim(),projectSummary:input.projectSummary.trim(),requestedSubdomain:input.requestedSubdomain.trim(),notes:input.notes.trim()}; if(!x.name) throw new Error("申请人名称不能为空"); if(!x.contact) throw new Error("联系方式不能为空"); if(!x.projectSummary) throw new Error("项目说明不能为空"); return x; }
export function verifyAdminAccess(auth:{emailHeader:string|null;cookieToken:string|null}, env:{ADMIN_EMAIL?:string;ADMIN_TOKEN?:string}): boolean { return Boolean((auth.emailHeader&&env.ADMIN_EMAIL&&auth.emailHeader.toLowerCase()===env.ADMIN_EMAIL.toLowerCase())||(auth.cookieToken&&env.ADMIN_TOKEN&&auth.cookieToken===env.ADMIN_TOKEN)); }

function postInput(fd: FormData): PostInput { const body=String(fd.get("body")??"").trim(); if(!body) throw new Error("动态正文不能为空"); return { id:String(fd.get("id")??"").trim()||undefined, title:String(fd.get("title")??"").trim(), body, slug:String(fd.get("slug")??"").trim()||undefined, tags:tags(String(fd.get("tags")??"")), status:String(fd.get("status")??"draft")==="published"?"published":"draft", pinned:["on","true","1"].includes(String(fd.get("pinned")??"")) }; }
async function appInput(req: Request): Promise<ApplicationInput> { const ct=req.headers.get("content-type")||""; if(ct.includes("application/json")) return createApplicationInput(await req.json() as ApplicationInput); const f=await req.formData(); return createApplicationInput({ name:String(f.get("name")??""), contact:String(f.get("contact")??""), projectSummary:String(f.get("projectSummary")??""), requestedSubdomain:String(f.get("requestedSubdomain")??""), notes:String(f.get("notes")??"") }); }
const id = (p:string) => `${p}_${crypto.randomUUID()}`;

export function createApp(repo: Repository) {
  const app = new Hono<AppEnv>();
  app.get("/", async (c) => c.html(home(sortPublishedPosts(await repo.listPosts()))));
  app.get("/api/feed", async (c) => c.json({ items: sortPublishedPosts(await repo.listPosts()) }));
  app.post("/api/applications", async (c) => { try { await repo.createApplication(await appInput(c.req.raw)); return c.json({ ok:true }, 201); } catch (e) { return c.json({ error:e instanceof Error?e.message:"提交失败，请稍后重试。" }, 400); } });
  app.get("/admin/login", (c) => c.html(login()));
  app.post("/admin/login", async (c) => { const token=String((await c.req.formData()).get("token")??""); if(!c.env.ADMIN_TOKEN || token!==c.env.ADMIN_TOKEN) return c.html(login("令牌不正确，请检查 ADMIN_TOKEN。"), 401); setCookie(c,"admin_token",token,{httpOnly:true,sameSite:"Lax",secure:true,path:"/",maxAge:60*60*24*14}); return c.redirect("/admin"); });
  app.use("/admin/*", async (c,next) => { if(c.req.path==="/admin/login") return next(); const ok=verifyAdminAccess({ emailHeader:c.req.header("CF-Access-Authenticated-User-Email")??null, cookieToken:getCookie(c,"admin_token")??null }, c.env); if(!ok) return c.redirect("/admin/login"); return next(); });
  app.get("/admin", async (c) => c.html(admin((await repo.listPosts()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), (await repo.listApplications()).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()), c.req.query("flash")||undefined)));
  app.post("/admin/posts", async (c) => { try { await repo.upsertPost(postInput(await c.req.formData())); return c.redirect("/admin?flash=动态已保存"); } catch (e) { return c.html(login(e instanceof Error?e.message:"保存失败"), 400); } });
  app.post("/admin/posts/:id/delete", async (c) => { await repo.deletePost(c.req.param("id")); return c.redirect("/admin?flash=动态已删除"); });
  app.post("/admin/applications/:id/status", async (c) => { const status=String((await c.req.formData()).get("status")??"new") as ApplicationStatus; await repo.updateApplicationStatus(c.req.param("id"), status); return c.redirect("/admin?flash=申请状态已更新"); });
  app.post("/admin/logout", (c) => { deleteCookie(c,"admin_token",{path:"/"}); return c.redirect("/"); });
  return app;
}

export function createD1Repository(db: D1Database): Repository {
  return {
    async listPosts() { const r=await db.prepare("SELECT id,title,body,slug,tags,status,pinned,created_at,updated_at FROM posts ORDER BY datetime(created_at) DESC").all<{id:string;title:string;body:string;slug:string;tags:string;status:PostStatus;pinned:number;created_at:string;updated_at:string}>(); return (r.results??[]).map((x)=>({ id:x.id,title:x.title,body:x.body,slug:x.slug,tags:x.tags?JSON.parse(x.tags):[],status:x.status,pinned:Boolean(x.pinned),createdAt:x.created_at,updatedAt:x.updated_at })); },
    async listApplications() { const r=await db.prepare("SELECT id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at FROM applications ORDER BY datetime(created_at) DESC").all<{id:string;name:string;contact:string;project_summary:string;requested_subdomain:string;notes:string;status:ApplicationStatus;created_at:string;updated_at:string}>(); return (r.results??[]).map((x)=>({ id:x.id,name:x.name,contact:x.contact,projectSummary:x.project_summary,requestedSubdomain:x.requested_subdomain,notes:x.notes,status:x.status,createdAt:x.created_at,updatedAt:x.updated_at })); },
    async createApplication(input) { const now=new Date().toISOString(); await db.prepare("INSERT INTO applications (id,name,contact,project_summary,requested_subdomain,notes,status,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, 'new', ?, ?)").bind(id("app"), input.name, input.contact, input.projectSummary, input.requestedSubdomain, input.notes, now, now).run(); },
    async upsertPost(input) { const now=new Date().toISOString(), pid=input.id||id("post"), slug=slugify(input.slug||input.title||pid); await db.prepare(`INSERT INTO posts (id,title,body,slug,tags,status,pinned,created_at,updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, COALESCE((SELECT created_at FROM posts WHERE id = ?), ?), ?) ON CONFLICT(id) DO UPDATE SET title=excluded.title, body=excluded.body, slug=excluded.slug, tags=excluded.tags, status=excluded.status, pinned=excluded.pinned, updated_at=excluded.updated_at`).bind(pid, input.title, input.body, slug, JSON.stringify(input.tags), input.status, input.pinned ? 1 : 0, pid, now, now).run(); },
    async deletePost(pid) { await db.prepare("DELETE FROM posts WHERE id = ?").bind(pid).run(); },
    async updateApplicationStatus(pid, status) { await db.prepare("UPDATE applications SET status = ?, updated_at = ? WHERE id = ?").bind(status, new Date().toISOString(), pid).run(); },
  };
}
